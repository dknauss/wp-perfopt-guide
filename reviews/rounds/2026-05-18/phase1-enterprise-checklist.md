# Phase 1 Review — Enterprise Checklist (`enterprise-performance-operational-checklist.md`)

## Structured Findings

| Document | Location | Finding | Severity | Recommendation | Verification |
|---|---|---|---|---|---|
| `enterprise-performance-operational-checklist.md` | Lines 10 and 779 (companion cross-reference links) | Both relative links point outside this repository (`../remkus-make-wordpress-fast/...`, `../wordpress-performance-master-reference/...`) and are currently broken in this checkout. | Medium | Replace with valid in-repo links, or label explicitly as external companion repos with full URLs and access expectations. | Run a link check (`markdown-link-check` or manual `ls`) from repo root and confirm links resolve. |
| `enterprise-performance-operational-checklist.md` | Line 12 (`@dknauss`) | Stray handle appears in document body, breaking editorial tone and checklist flow. | Low | Remove or move attribution into front matter/changelog context. | Preview Markdown and confirm section renders without orphan token. |
| `enterprise-performance-operational-checklist.md` | Lines 133–141 (`x_redirect_by` debug filter example) | Example uses `wp_die( var_dump(...) )`; `var_dump()` returns `null` and emits output as side effect. This is a brittle debugging pattern and easy to misuse on live traffic. | Medium | Replace with safer guarded debugging pattern (e.g., `error_log( wp_json_encode(...) )` behind env/capability checks) and explicitly require non-production scope. | Validate behavior in a local sandbox request and confirm no user-facing fatal output path remains. |
| `enterprise-performance-operational-checklist.md` | Lines 246–257 (partial output cache example) | Cache key uses only `get_current_blog_id()`, which under-specifies context (locale, template context, query args, auth/personalization state). This can serve incorrect fragments across contexts. | High | Expand key design guidance to include all output-varying dimensions and add explicit “never share personalized fragments” keying examples. | Reproduce with two distinct contexts (e.g., locale/category/user state) and confirm unique keys prevent fragment bleed. |
| `enterprise-performance-operational-checklist.md` | Lines 535–552 (`DISABLE_WP_CRON` and cron runner) | Guidance can be read as “disable WP-Cron now,” but does not make the sequencing safety requirement explicit: disabling before verified external cron causes missed scheduled events. | High | Add explicit precondition: verify external cron is active and monitored **before** setting `DISABLE_WP_CRON`. Include rollback step. | In staging, disable WP-Cron only after runner is installed; verify due events execute (`wp cron event list/run --due-now`) and no backlog grows. |
| `enterprise-performance-operational-checklist.md` | Lines 703–706 (New Relic example) | Example starts/ends a transaction inside request code. New Relic docs frame `newrelic_start_transaction()` primarily for starting a new transaction after manual end (e.g., job queue separation), so this is easy to misapply for normal web traces. | Medium | Replace with safer APM instrumentation examples (naming/segments/custom parameters) and add “use start/end transaction only for advanced non-default transaction lifecycle control.” | Cross-check against New Relic PHP agent API docs for `newrelic_start_transaction()` and validate resulting transaction visibility in APM before/after snippet change. |
| `enterprise-performance-operational-checklist.md` | Line 777 (Appendix title: “current to WordPress 6.8, May 2026”) | Version framing is stale for the stated date. As of **May 18, 2026**, WordPress 6.9.x is current stable (6.9.4), with 7.0 in RC cycle. | High | Update appendix scope to currently supported branch(es) and add an explicit “last verified on YYYY-MM-DD” stamp. | Verify against WordPress release archive and official release posts, then update date/version statement. |
| `enterprise-performance-operational-checklist.md` | Lines 800–809 (Speculative Loading subsection) | Contains factual drift: says Core 6.8 defaults to “prerender on mousedown” and suggests `data-prefetch="false"`. Core 6.8 default is `prefetch` + `conservative` eagerness; common opt-out uses `no-prefetch`/`no-prerender` classes or filters. | High | Correct defaults and opt-out mechanisms; separate Core behavior from feature-plugin behavior, and cite the 6.8 dev note. | Validate against Make/Core “Speculative Loading in 6.8” dev note and test generated speculation rules markup on a 6.8+ site. |
| `enterprise-performance-operational-checklist.md` | Sections 4, 12, 13 (cache layer terminology coverage) | Checklist is strong on edge/object/transients but omits explicit differentiation of `WP_CACHE`, `advanced-cache.php`, and `object-cache.php` drop-ins; this risks reader conflation in enterprise incidents. | Medium | Add a short “cache layer boundaries” subsection clarifying what each constant/drop-in controls and what it does **not** control. | Review against WordPress Developer Docs and confirm terminology matches drop-in responsibilities. |
| `enterprise-performance-operational-checklist.md` | Lines 818–825 (Performance Lab module list) | Module list is version-sensitive and likely to drift; presented as static guidance without plugin-version qualifier. | Medium | Add plugin-version/date qualifier or convert to “examples, verify current modules before rollout.” | Compare listed modules with current Performance Lab readme/changelog at review time and update discrepancies. |

## Reviewer Notes

- Audit scope intentionally limited to `enterprise-performance-operational-checklist.md` per assignment.
- Focus applied: measurement-first guidance, cache-layer precision, WordPress API accuracy, and operational safety.
